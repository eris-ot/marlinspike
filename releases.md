# MarlinSpike — Releases

## Versioning

Engine and web UI are now versioned separately:
- **Engine** (`marlinspike/engine.py`, formerly `_ms_engine.py`): versions tracked in the table below. Bumped on analysis chain / tshark / report format changes. Triggers Cython rebuild.
- **Web UI** (`marlinspike/app.py` + `marlinspike/templates/`): `APP_VERSION` in `marlinspike/app.py`. Bumped on UI/UX changes. Deployable independently via `./deploy.sh --ui-only` (no rebuild).

## Engine Releases

| Version | Date | Changes |
|---------|------|---------|
| v2.0.9 | 2026-04-25 | **Bilgepump L2 anomaly consumer + per-packet ARP observations.** `_ms_engine.py` now consumes `parse_anomaly` events from `marlinspike-dpi`'s `bilgepump` subsystem (decoders prefixed `bilgepump:`) and surfaces them as a sorted `l2_anomalies` list on the report — covers `arp_spoof`, `arp_gratuitous`, `mac_flap`, `mac_local`, and `mac_multicast`. New `arp_observations` field captures per-packet ARP frames (`arp.opcode`, `arp.isgratuitous`, src/dst MAC and IP) on the tshark dissection path so downstream sidecar plugins get full per-packet fidelity that single-conversation aggregation collapses. `MarlinSpikeReport` extended with `arp_observations`, `arp_observations_truncated`, and `l2_anomalies` fields. Bronze consumer contract documented at `docs/bronze-consumer-contract.md`. Engine API surface unchanged; both fields default to empty lists when not applicable. |
| v2.0.8 | 2026-04-15 | **ICS ATT&CK rule pack + multi-pack MITRE loading.** Catalog and plugin dispatch now load all `*.yaml` packs from `rules/mitre/` instead of a single `base.yaml`. ICS ATT&CK domain lookups added alongside enterprise-attack. New `ics-attack.yaml` pack: 33 rules across 12 families — passive recon coverage (T0842, T0840, T0888, T0801, T0868, T0861), controller manipulation (S7comm program upload/download/tasking, Modbus unauthorized writes), cross-Purdue zone traversal, OPC-UA without security, cleartext engineering access, default/hardcoded credentials, internet-accessible OT devices, C2 beaconing, DNS exfiltration, port scan discovery, IT services on OT devices, cleartext remote access, and malware IOC matches. Per-rule domain labeling in catalog entries. Combined version label: Enterprise 18.1 / ICS 18.1. Total coverage: 42 rules, 31 unique techniques across both packs. |
| v2.0.7 | 2026-04-13 | **Rust DPI engine enabled by default.** Web UI scan handler now passes `--dpi-engine auto` to the engine, using `marlinspike-dpi` (Rust) when the binary is available and falling back to Python tshark otherwise. Both the direct scan path and the chunked chain dissect path are wired. Configurable via `MARLINSPIKE_DPI_ENGINE` and `MARLINSPIKE_DPI_BIN` environment variables. **Benchmark (CTU-13, 1.7 GB, 2.45M packets):** Rust DPI = 58 seconds, Python tshark = 13.8 minutes (14x speedup). |
| v2.0.6 | 2026-04-12 | **Auth hardening + audit logging (backported from fathomips).** Password reset tokens: SHA-256 hashed, 30-minute TTL, single-use, IP-logged. `POST /api/auth/reset-request` and `POST /api/auth/reset-confirm` endpoints. Session invalidation: `session_version` field on User model — `change_password()` and `use_reset_token()` bump the version, stale sessions rejected on next `login_required` check. Immutable audit logging (`_audit.py`): tracks logins, failed logins, logouts, token issued/rejected/used. `PasswordResetToken` and `AuditLog` models with auto-migration on startup. Expired token cleanup runs on boot. |
| v2.0.4 | 2026-04-11 | **Remove silent PCAP truncation.** The upload handler was silently byte-truncating any PCAP over 100 MB (`PCAP_PROCESS_SIZE`) before the scan engine ever saw it — a pre-v1.7 memory protection made obsolete by the v2.0.0 chunked pipeline. Removed the entire auto-slice block. Defaults raised: `PCAP_MAX_SIZE` and `PCAP_PROCESS_SIZE` both now 5 GB. Per-user `upload_limit_mb` still enforces individual caps. |
| v2.0.3 | 2026-04-11 | **OUI database duplicate fix (credit: Michael Sargis — [PR #3](https://github.com/eris-ot/marlinspike/pull/3)).** `ICS_OUI_DB` had duplicate keys for `00:50:c2` — first mapped to Honeywell (C300, Safety Manager), second mapped to GE (PACSystems RX3i). Python dict semantics silently overwrote the Honeywell entry, causing Honeywell devices to be misidentified as GE. Removed the incorrect GE entry from both `_ms_engine.py` and `msengine/msengine/engine.py`. A correct GE PACSystems RX3i OUI should be sourced from IEEE and added separately if needed. |
| v2.0.2 | 2026-04-11 | **Live capture removed.** Removed `start_live_capture()`, the `--interface` / `--capture-duration` / `--capture-filter` CLI flags, and the `list-interfaces` subcommand from the engine. `CaptureIngestor` now accepts only `pcap`. Engine module metadata updated: `pcap` is now a required parameter. MarlinSpike is now a PCAP-only analysis tool — for continuous live capture and centralized OT monitoring, use [FATHOM](https://github.com/eris-ot). Companion changes in 2.0.2 web UI release. |
| v2.0.1 | 2026-04-10 | **Classification fixes from community testing (credit: Jerrid Brown).** `_is_public_ip()` now excludes multicast (224.0.0.0/4) so DCS multicast traffic no longer triggers CRITICAL external-comms findings on air-gapped networks. `_fingerprint_vendors()` skips external hosts (Purdue 5 / asset_type "external") to prevent gateway MAC OUI being attributed to remote IPs. `_check_no_auth()` rewritten with protocol-context filtering: only flags devices using auth-capable protocols (SSH, HTTP, Modbus, OPC-UA, etc.) — ARP/mDNS/DHCP/ICMP/LLDP exempt; previously `auth_observed` was never set so 100% of nodes were flagged. Level 3 "Application Server" catch-all split into **Data Server** (DB/file services), **Infrastructure Server** (with service-specific device types: Web Server, Network Services Host, Mail Server, Monitoring Server), and **Operations Host**. Purdue level inference split into two passes: OT devices first, then non-OT classified as Level 3 only if they communicate with OT assets (L0-L2), otherwise Level 4 (enterprise IT). New Level 4 roles: Enterprise Server, Enterprise Workstation, Enterprise Host. Stale `marlinspike-malware` ref updated (02eb369c → 4a008211). |
| v2.0.0 | 2026-03-27 | **Release 2.0.0 — fast/full profiles, chunk-friendly continuation, and stable post-dissect chaining.** Added scan profile support to the engine (`--fast` plus legacy command alias normalization), introduced `chain-from-conversations` so topology and risk can continue from merged Stage 2 conversation artifacts, and preserved the existing report contract while making large-capture orchestration practical above the engine. Fast mode now applies lower-cost defaults such as skipping ephemeral edges, lowering inline collapse thresholds, and omitting expensive C2 heuristics when requested. **Stage 4b: Malware IOC Matching** — `marlinspike-malware` Rust engine integrated as optional post-risk-scoring phase. When binary and rules directory are auto-discovered, conversations are evaluated against 919 detection rules across 29 threat categories (APT tooling, ransomware, C2 frameworks, ICS/OT threats, botnets, credential theft, etc.). Findings merge into `c2_indicators` (type `MALWARE_IOC_MATCH`) and `risk_findings`. Skipped in `--fast` mode. Report gains `malware_findings` field. |
| v1.9.1 | 2026-02-26 | **Vendor infrastructure fix + demo refresh.** Added `"phoenix contact"` and `"innominate"` to `_assign_roles()` vendor infrastructure check — Phoenix Contact FL SWITCH and Innominate mGuard appliances appearing in IT-protocol-only traffic are now correctly classified as "Network Infrastructure" instead of "Historian/MES". Demo report (`demo/demo-report.json`) regenerated with current binary: Network Infrastructure 0 → 35, Historian/MES 31 → 18 on 4SICS benchmark. Demo dir restructured: `COPY demo/ ./demo/` (was `data/demo/`) — `demo/` is now the local source of truth, synced by `deploy.sh`. Dual-synced to `VORACITY-MODULE-MARLINSPIKE.py` |
| v1.9.0 | 2026-02-25 | **L2 node merge + MAC table.** `_merge_l2_nodes()` merges MAC-only nodes (from LLDP/STP L2 packets) into IP-keyed counterparts — fixes switches being misclassified as "Engineering Workstation" due to MAC/IP node split. `_build_mac_table()` builds MAC-to-IP recon table with vendor/capabilities/source enrichment, exposed as `mac_table` field in report JSON. Viewer: tab bar in report pane ("Report" / "MAC Table"), sortable MAC table with color-coded source badges. ROLE_STYLES: added Network Switch + Router entries with prefix-matching fallback for named roles |
| v1.8.0 | 2026-02-25 | **Dashboard truncation + dedicated Reports & Scans pages.** Dashboard now shows only the 10 most recent reports and scans with "View all" links. New `/reports` page (full report list with project filter, compare/diff, view/download/delete). New `/scans` page (full scan history with project filter, retry, error details). Nav bar updated: Dashboard, Reports, Scans, Projects. API: `/api/reports` and `/api/history` accept optional `limit` query param; `/api/reports` returns `total` count |
| v1.7.0 | 2026-02-24 | **Security hardening + scalability overhaul.** XSS fix (`\|safe` → `\|tojson` in 3 templates). Path traversal fix (pathlib resolve + prefix check). CSRF origin check middleware. Rate limiting (flask-limiter). Session cookie hardening (Secure/HttpOnly/SameSite=Lax). Error message sanitization. Security event logging. Password 8-char minimum. `.env.backup` deleted and deployment secrets rotated. **Memory fix:** TCP reassembly disabled in all tshark invocations (`tcp.desegment_tcp_streams:FALSE`, `ip.defragment:FALSE`) — Stage 1 drops from 1.7 GB to ~300 MB. Both Stage 1 tshark passes (address extraction + io,phs protocol hierarchy) eliminated — MACs, IPs, and protocols deferred to Stage 2 dissection results; capinfos provides packet count/duration. **Inline unique conversation reduction:** port scan traffic collapsed by MAC pair during Stage 2 tshark processing when >N unique dest ports (default 50) — 1.8M port-scan packets merged inline, 473K raw → 2,205 unique conversations on 200 MB PCAP (2.2M packets). **Benchmark (200 MB 4SICS):** 2 min 56 sec, 395 MB peak memory, 69 MB idle (was 35 min / 3 GB OOM). **New CLI options:** `--chunk-size N` (editcap-based PCAP splitting), `--collapse-threshold N` (port scan dedup), `--reassembly` (opt-in TCP reassembly). WebUI defaults: chunk 300K, collapse 50. `MAX_CONCURRENT_SCANS` reduced to 1 (sequential queue) |
| v1.6.0 | 2026-02-24 | **Sample PCAP Library + 50 MB upload limit.** 25 curated ICS/OT PCAPs from Malcolm-Test-Artifacts (Idaho National Lab) baked into Docker image under `presets/`. 4 categories: ICS/OT Protocols (16), IT Reference (4), Digital Bond S4 (4), CTF (1). New "Sample Library" tab on dashboard with browsable category cards. Upload limit reduced from 500 MB → 50 MB. Preset path resolution with `preset:` prefix, retry fallback searches preset subdirs |
| v1.5.0 | 2026-02-23 | **Stage 3/4 performance fix.** Edge merging collapses 358K edges → ~hundreds by `(src, dst, protocol, dst_port, transport)` key. Conversation indexes (`_conv_by_src`/`_conv_by_dst`) eliminate O(n*m) nested loops in `_fingerprint_vendors()` and `_assign_roles()`. Cython `defaultdict` type annotation fix in `save_yaml_map()`. Stage 3 drops from 20+ min to seconds on 200 MB PCAPs |
| v1.4.0 | 2026-02-23 | **Five-tuple port analysis + C2 detection.** Full five-tuple extraction (src/dst IP, src/dst port, transport) on all conversations — non-OT traffic no longer dropped. `WELL_KNOWN_PORTS` database (70+ IT/OT services). Service port profiles per node, port labels per edge. Beacon detection via IAT histogram clustering (jitter-resistant, adaptive bin width). DNS exfiltration detection via Shannon entropy of subdomain labels. Asymmetric flow analysis, suspect C2 channel detection, connection persistence scoring. Port risk analysis: cleartext remote access, IT-on-OT, unknown high-port services. Ephemeral port filtering (>= 49152 w/ single connection). `NO_AUTH_OBSERVED` suppressed from per-asset views. New MITRE ATT&CK coverage: T1071, T1573, T1048, T1132, T1029. New report fields: `port_summary`, `c2_indicators`, node `service_ports`, edge `dst_port`/`port_label`/`transport` |
| v1.3.0 | 2026-02-23 | Assessment Report pane in viewer: collapsible bottom drawer with executive summary, risk findings with remediation, detailed asset inventory, protocol breakdown, Purdue violations. Google OAuth. User model extended with `email` + `oauth_provider` |
| v1.2.1 | 2026-02-23 | Fix tshark L2 field names for tshark 4.4.x (LLDP, CDP, LACP) — Stage 2 no longer errors on `lldp.tlv.system_cap`, `cdp.native_vlan`, `lacp.actor.*` fields. Removed non-existent `cdp.addr.ip` field |
| v1.2.0 | 2026-02-23 | Live streaming mode (incremental topology snapshots during capture), topology diff engine, inline L2 parsers for LLDP/CDP/STP/LACP, `_classify_protocol()` encap-aware stack walker |
| v1.1.0 | 2026-02-22 | L2 protocol discovery: LLDP (chassis, ports, capabilities, VLANs), CDP (device ID, platform, native VLAN), STP/RSTP (root/bridge MAC, port cost, topology change), LACP (actor/partner system, port, key). Frame protocols stack-based classification. Conversation-level OT metadata enrichment |
| v1.0.0 | 2026-02-22 | Initial standalone extraction from Voracity module. 5-stage chain: capture ingest, OT protocol dissection (Modbus, EtherNet/IP, CIP, S7comm, DNP3, OPC-UA), topology build with Purdue level inference, risk surface scoring, report generation. CLI + importable API. GrassMarlin bridge (optional) |

## Web UI Releases

| Version | Date | Changes |
|---------|------|---------|
| v3.6.0 | 2026-05-18 | **Enrichment moved into the engine — headless `chain` now emits a complete report.** MITRE / ARP / APT / CISA orchestration was previously embedded in `app.py:_finalize_run` (web-only), so any headless consumer of `engine.py chain` (engine-svc, cloudmarlin, CI, batch) silently produced *under-enriched* reports. New Flask-free `marlinspike/enrich.py` owns the single implementation (the four `python -m <plugin>` runners de-duplicated behind one spec table, 120 s per-plugin timeout, per-plugin `MARLINSPIKE_*_ENABLED` gating, `rules/<name>/*.yaml` + explicit-rules sweep — all unchanged). `engine.py` gains an `enrich` subcommand and `--enrich/--no-enrich` (default **on**); `chain` and `chain-from-conversations` now run enrichment and rewrite the report in place so `report.json` is self-complete (`extensions` + plugin-sourced `risk_findings` merged). `write_enriched()` / `load_report_with_extensions()` are **idempotent** — prior plugin-sourced findings are stripped before re-merge, so `chain --enrich` and a later standalone `enrich` agree. `app.py` now delegates to `marlinspike.enrich` via thin wrappers (357 duplicated lines removed; web behavior unchanged). **No schema/migration change**; old reports without `extensions` remain valid. **Behavior change for headless callers:** `chain` now enriches by default — pass `--no-enrich` to restore the prior core-only output (a broken/slow plugin never fails the chain; it is logged and skipped). Smoke-verified end-to-end on a real ICS pcap via `marlinspike-dpi`: `--no-enrich` → no `extensions`; `enrich` → sidecar merged, exactly one plugin-sourced finding; re-run stable (idempotent). `test_cisa_plugin` green. |
| v3.5.6 | 2026-05-18 | **Fix 500 on `/capture`, `/iocs`, `/findings`, and base-template chips (taxonomy macro context).** `_macros.html`'s `tax_chip` / `sev_chip` macros read `taxonomy_entities` and `severity_chip_class`, which are supplied by the `inject_taxonomy` **context processor**. The four templates that import them — `capture.html`, `iocs.html`, `findings.html`, `base.html` (footer chips) — used a bare `{% from "_macros.html" import tax_chip, sev_chip %}`, and Jinja imports without `with context` only see jinja globals, not context-processor variables. Result: every page that actually invoked a chip raised `UndefinedError: 'taxonomy_entities' is undefined` → HTTP 500. All four imports (plus the `_macros.html` usage doc) now use `... import tax_chip, sev_chip with context`, honoring the macro's documented contract. No schema/API/config change; UI-only, deployable via `./deploy.sh --ui-only`. |
| v3.5.5 | 2026-05-16 | **Project sharing + CISA advisory enrichment + shared-backend rate limiting.** **Project sharing** — projects can now be shared beyond their creator. New `project_members` join table (Alembic migration `0002`) and `ProjectMember` model with `viewer | editor | owner` roles; the creator's ownership stays implicit in `projects.user_id` and is never stored here. A single `_get_project_for_user(pid, min_role)` role gate replaces the previous creator-only `Project.query.filter_by(user_id=…)` scoping across every project-scoped surface (asset tags, finding notes, files, reports, IOC lists, OCSF/Navigator/STIX/Sigma emit, aggregate, baseline) — reads require `viewer`, mutations `editor`, project rename/delete and membership management `owner`. File/report path resolution now keys off the project creator's id (`proj.user_id`) so shared members resolve the owner's storage, not their own. New membership CRUD API (`GET/POST /api/projects/<pid>/members`, `PUT/DELETE /api/projects/<pid>/members/<uid>`) — owner-only for mutations, creator row protected from role-change/removal, add is upsert. `GET /api/projects` now returns shared projects too, each tagged with `is_owner` and `member_role`. **CISA advisory enrichment** — new bundled `marlinspike-cisa` plugin (`plugins/marlinspike_cisa/`) searches a locally-synced CISA catalog (KEV + ICS advisories, 1,622-entry snapshot shipped) by report-derived asset/vendor/CVE terms and emits a `*-cisa.json` sidecar in the standard plugin envelope. `chain` runs it as a final stage when `MARLINSPIKE_CISA_ENABLED` (default on); `_load_report_with_extensions()` merges the sidecar under `extensions["marlinspike-cisa"]`. New `rules/cisa/base.yaml` keyword-group pack for common ICS/OT vendors; `MARLINSPIKE_CISA_{ENABLED,MODULE,RULES}` config. **Shared-backend rate limiting** — optional `RATELIMIT_STORAGE_URI` points flask-limiter at a shared store (e.g. `redis://…`) so auth/upload throttles survive restarts and span Gunicorn workers; a startup health check **refuses to boot** if a configured backend is unreachable (fail-closed rather than silently ineffective), and an unset URI logs a single-process-only warning and falls back to in-memory. New `redis>=5.0` dependency. **Password policy** now also enforced on the reset-confirm and admin-create paths (previously only the legacy 8-char floor). New sharing/CISA code uses SQLAlchemy 2.0 `Session.get()` and timezone-aware datetime defaults. **Tests:** 366 pass (340 baseline + 19 project-sharing + 7 CISA-integration). **No breaking changes.** Existing deployments: run `marlinspike-db upgrade head` to apply migration `0002`. **Still pre-existing / tracked:** legacy `app.py` `Query.get()` / `datetime.utcnow()` call sites remain (bracketed for the v3.6 monolith split, per CLAUDE.md); coverage-gate baseline threshold still deferred. |
| v3.5.4 | 2026-05-10 | **Architectural hardening — Alembic migrations + CSRF token + live-capture per-project policy + type checking + coverage + password policy.** Six items from the post-v3.5.3 backlog shipped together, three of them produced by parallel architect-engineer agents in isolated git worktrees. **Alembic migrations replace `db.create_all() + ad-hoc ALTER TABLE`** as the schema-evolution mechanism. New `migrations/` tree with `alembic.ini`, `env.py`, and a baseline migration capturing all 11 application tables including the v3.4 recovery columns and the new v3.5.4 `projects.capture_policy` column. `create_app()` now calls `_run_schema_init()` which dispatches: persistent DB → `flask_migrate.upgrade()`; in-memory SQLite → `db.create_all()` (Alembic per-connection isolation incompatible with `:memory:`); `MARLINSPIKE_ALLOW_NO_DATABASE_URL=true` → `db.create_all()` (test mode); no `migrations/` dir → `db.create_all()` fallback (bare wheel install). New `marlinspike-db` console script wraps `flask db {upgrade,downgrade,current,stamp,migrate}`. New `migrations` optional-dep group (`pip install marlinspike[migrations]`). Existing v3.5.x deployments must run `marlinspike-db stamp head` once before upgrading — fully documented in UPGRADING.md. **Proper per-session CSRF token replaces origin-only check** as the primary defense (origin check retained as defense-in-depth — v3.5.2 MS-2026-006 stays active). New `marlinspike/csrf.py` with `csrf_token()` / `validate_csrf()` (constant-time `secrets.compare_digest`); token minted lazily on first access (32 bytes via `secrets.token_urlsafe`), rotated on every login, destroyed with the session on logout. `before_request` hook reads `X-CSRF-Token` header or `_csrf` form field; valid token short-circuits the rest; invalid/absent token falls through to origin check; both failing → 403. Jinja `csrf_token()` injected globally via context processor; `<meta name="csrf-token">` in `base.html`; `window.fetch` monkey-patched to auto-add the header on non-GET. `@csrf_exempt` decorator (already existed) skips both checks for webhook endpoints. Login is exempt — no session before login. **Live-capture per-project policy + system interface allowlist** — gates at start_session() in order: (1) `project.capture_policy.enabled==false` → 403; (2) effective interface allowlist = system `MARLINSPIKE_CAPTURE_INTERFACE_ALLOWLIST` ∩ `policy.allowed_interfaces` (each side optional, empty intersection → 403); (3) `max_session_duration_s` clamps requested duration silently with `audit("capture.policy_capped", ...)`; (4) `operator_warning` passed through in 201 response, UI shows confirmation modal. New `GET/PUT /api/capture/policy/<pid>` (admin or project owner). New `Project.capture_policy` TEXT column (JSON-encoded). Workbench capture page filters interface dropdown to the effective allowlist. **Password policy strengthened**: minimum length raised 8 → 12; require two of (upper, lower, digit, symbol); reject explicit deny-list of common predictable passwords. Both password-change endpoints (`/api/users/<u>/password` and `/api/account/password`) get `@limiter.limit` (10/hr admin, 5/hr self-service) plus `audit("auth.password_change*", ...)` events on success and failure paths. **Type checking** (`pyproject.toml [tool.mypy]` + CI step): strict on new modules (taxonomy, run_store, recovery, setup_wizard, emit/{ocsf,navigator,stix,sigma}); legacy `app.py` and `engine.py` excluded pending the v3.6+ split. New `typecheck` pyproject extra. **Coverage gate** (`pyproject.toml [tool.coverage]` + CI step): branch coverage on; `app.py` / `engine.py` omitted from coverage (tracked separately); CI generates `coverage.xml` artifact; gate threshold deferred to v3.5.5 once baseline is measured. New `pytest-cov` test-extra. **Tests:** 336 total pass (297 baseline + 13 CSRF + 5 Alembic + 21 capture-policy). 39 new tests across the four agent areas. **No breaking changes for fresh deployments.** Existing deployments running v3.5.x: stamp the database with `marlinspike-db stamp head` before upgrading. **Tracked for v3.5.5+:** baseline coverage threshold, password-change MFA, capture-policy max_total_bytes enforcement (column exists, capd integration deferred), per-user (sub-project) capture policy. **v3.6.0** still has the monolith split (app.py + engine.py per-domain modules) as headline. |
| v3.5.3 | 2026-05-10 | **Drop msengine/ subtree.** The `msengine/` directory was a subtree-vendored bootstrap of a future standalone `marlinspike-msengine` repo that was never extracted. The sync script (`scripts/sync-msengine-bootstrap.sh`) had been broken since v3.0.0 (still pointed at the pre-package-restructure path `_ms_engine.py` which has not existed since), so the snapshot drifted ~636 lines behind the operational engine while nothing surfaced the breakage. Three failure modes — an absent extraction target, a dead sync mechanism, and a stale-rotting copy — all pointing at "this thing isn't earning its keep." Removed: `msengine/` (1.9MB, 5167 lines), `scripts/sync-msengine-bootstrap.sh`. Updated `SUITE-MANIFEST.yaml` to mark the component `dropped` with provenance, `CLAUDE.md` to remove "subtree-vendored bootstrap" references. The operational source of truth has always been `marlinspike/engine.py`; if and when the standalone repo is extracted, it pulls from there at that point via `git filter-repo`. **The architectural hardening list outlined for v3.5.3** (Alembic migrations, type checking, coverage gate, proper CSRF token, password-change audit + rate limit, live-capture per-project policy + interface allowlist) was scoped out of this patch and tracked for v3.5.4+ — they're each a real piece of work and bundling them with the msengine cleanup made the patch unfocused. v3.5.3 is the cleanup-only release. 297/297 tests still pass. |
| v3.5.2 | 2026-05-10 | **Security patch — nine fixes (one CRITICAL, three HIGH, five MEDIUM) + setup wizard + lint config + version unification.** Full advisory list in [SECURITY.md](SECURITY.md). Headlines: **MS-2026-001 (CRITICAL):** `/api/auth/reset-request` no longer returns the reset token in the response — pre-v3.5.2, anyone who knew a username could trigger account takeover. Token delivery now configurable via `MARLINSPIKE_RESET_TOKEN_DELIVERY` (default `disabled`). **MS-2026-002 (HIGH):** live-capture session start/stop now requires admin role by default (`MARLINSPIKE_CAPTURE_REQUIRE=admin`). **MS-2026-003 (HIGH):** sub-PCAP extraction fails closed — `tshark` / `editcap` failures return HTTP 500 instead of falling back to the original (full) PCAP. **MS-2026-004 (HIGH):** removed predictable default `DATABASE_URL=postgresql://marlinspike:marlinspike@...`; `create_app()` refuses to start without `DATABASE_URL` set. **MS-2026-005 (HIGH):** removed silent ephemeral `SECRET_KEY` generation; refuses to start without it set. **MS-2026-006 (MEDIUM):** CSRF check now compares full origin (scheme+host+port) instead of hostname-only. **MS-2026-007 (MEDIUM):** first-run admin password written to `data/instance/admin-bootstrap-password.txt` mode 0600 instead of printed to stdout. **MS-2026-008 (MEDIUM):** real browser security headers — per-request CSP nonce, `frame-ancestors 'none'`, `object-src 'none'`, `X-Content-Type-Options: nosniff`, `X-Frame-Options: DENY`, `Referrer-Policy: strict-origin-when-cross-origin`, `Permissions-Policy` lockdown, HSTS when `SESSION_COOKIE_SECURE`. **MS-2026-009 (MEDIUM):** `SESSION_COOKIE_SECURE` defaults to True. **New `marlinspike-setup` wizard** (`marlinspike/setup_wizard.py`, ~250 LOC) bootstraps a fresh deployment: generates a strong SECRET_KEY (256-bit), proposes a SQLite or PostgreSQL DATABASE_URL, generates an admin password, sets the security-relevant defaults, and writes them all to `.env` with mode 0600. Run as `marlinspike-setup` (interactive) or `marlinspike-setup --auto` (non-interactive, accept all defaults). New `[project.scripts]` entry; new `--print-only` mode for piping into a config-management tool. **Lint config** added: `ruff` with sensible defaults + bandit-style security checks (`S`) selected; new modules (emit/, recovery, run_store, taxonomy, setup_wizard) lint cleanly and CI fails on regressions; legacy `app.py` + `engine.py` warned but not failed pending the v3.6+ split. New `dev` and `lint` extras in pyproject. **Version unification** — pre-v3.5.2 had drift across `pyproject.toml` (3.5.1), `__init__.py` (3.5.1), `app.py:APP_VERSION` (3.2.0), `README.md` (3.2.0), `README.fr.md` (3.2.0). All five sources now agree at 3.5.2. **`admin_required` decorator** in `marlinspike.auth` now smart: returns JSON 401/403 for `/api/*` paths, redirect/HTML for everything else. Used by capture/api.py + reusable for any future admin-gated endpoint. **Tracked for v3.5.3+** (architectural; not in this patch): proper CSRF token, password-change audit + rate limit, live-capture per-project policy + interface allowlist, Alembic migrations, app.py/engine.py split, removing inline `style=`/`onclick=` so `'unsafe-inline'` can drop from CSP. **Tests:** 21 new (test_security_v352) covering each fix's positive + negative cases. 297 total pass. **Migration note:** existing deployments must set `DATABASE_URL` and `SECRET_KEY` before upgrading or set `MARLINSPIKE_ALLOW_NO_DATABASE_URL=true` + `MARLINSPIKE_ALLOW_GENERATED_SECRET=true` (test/dev only). New deployments: run `marlinspike-setup` once. |
| v3.5.1 | 2026-05-10 | **Patch release — fix v3.5.0 source archive self-consistency + broken CI + missing operator docs.** v3.5.0 was tagged with `pyproject.toml` still at `3.2.1`; the published source archive's `pip install` metadata showed `3.2.1` while `import marlinspike; marlinspike.__version__` showed `3.5.0`. v3.5.1 aligns both at `3.5.1`. **Also fixes `.github/workflows/ci.yml` and `release.yml`** which referenced pre-v3.0.0 module paths (`app.py`, `_auth.py`, `_config.py`, `_models.py`, `_ms_engine.py`) and had been silently failing CI on every push for 6+ releases; both workflows now install with `pip install -e .[test]` and run pytest plus a smoke-import of the public API. **INSTALL.md** gains a "Mid-scan recovery (v3.4.0+)" section documenting `MARLINSPIKE_RUN_STORE` (memory/db, set to `db` for multi-worker Gunicorn so per-tier concurrency limits stay correct cross-worker) and `MARLINSPIKE_SCAN_TIMEOUT_S` (default 3600s, 0 to disable abandonment reaping). **`docs/workbench-guide.md`** had ~290 lines of pre-v3.5 "Left-rail modes" + "Right sidebar" content sitting after the new v3.5 layout section, contradicting it; replaced with a single deprecation note + a mapping table showing which v3.5 surface replaces each pre-v3.5 surface (Map mode → Comms lens, Findings mode → Findings lens + drawer Findings tab, Selected Asset sidebar → Inspector, etc.). Historical content remains accessible at git tag `v3.4.0`. **New `SECURITY.md`** — disclosure email, ERISFORGE Ltd. GPG key fingerprint for encrypting reports, response timeline (72h ack, 7d triage, 30-90d default disclosure window), scope (in/out), supported versions matrix (3.5.x current, 3.4.x recent, 3.3.x best-effort, ≤3.2.x and 2.x unsupported), and a threat-model summary covering the capd sidecar trust boundary and the engine subprocess as PCAP-input boundary. No code or schema changes. 192/192 tests pass. |
| v3.5.0 | 2026-05-09 | **Map-first workbench reshape + multi-lens relational graphing + formal taxonomy + HP-HMI mode.** The analyst workbench (`marlinspike/templates/viewer.html`) was rebuilt around a persistent map canvas instead of the legacy left-rail mode tabs. New skeleton: top bar with report meta + time-scrubber timeline; **lens chip strip** below it with six lenses (Comms / Findings / IOC / ATT&CK / Baseline / Peers); persistent **map canvas** as the primary 60-70% of the viewport; **dockable inspector** on the right (collapsible) showing taxonomy-driven entity detail (chip, properties grid, observed protocols, neighbors, findings, tags/notes, pivot strip); **slide-up bottom drawer** with seven tabbed tables (Findings / Conversations / Assets / IOCs / Anomalies / ATT&CK / DNS) where click-row → highlights on map. Map is the truth, everything else is a lens or projection onto it. **Six real lenses, no stubs:** Comms (current host-host conversations), Findings (severity-sorted card overlay with click-to-pivot to affected nodes), IOC (matched-asset halo + per-match cards), ATT&CK (tactic-grouped technique grid for ICS + Enterprise domains, live data from the `marlinspike-mitre` plugin output via `VIEWER_CONTEXT.mitre_classifications` + `mitre_matrix_domains`, click technique → highlights affected assets), Baseline (per-asset cards for top-12 most-connected assets, each fetching `/api/projects/<pid>/assets/<key>/baseline` async with novelty highlights `+N peers / +N protocols / +N findings / -N lost peers` and severity-colored borders), Peers (Role / Vendor / Purdue Level grouping with anomalous-by-context flagging for assets unique at their Purdue level). **Formal entity taxonomy** in new `marlinspike/taxonomy.py` (12 entities — Asset / Conversation / Finding / Anomaly / C2Indicator / MalwareFinding / IocList / IocEntry / Project / Report / CaptureSession / Protocol — and 12 relationships — `communicates_with` / `affected_by` / `flagged_by_anomaly` / `flagged_by_c2` / `flagged_by_malware` / `matched_by_ioc` / `initiated_by` / `received_by` / `generates` / `belongs_to_project` / `contained_in_report` / `ioc_in_list`); each entity carries a visual spec (color CSS var, icon SVG, node shape, chip class, i18n label keys) used by both Jinja and JS rendering paths. New `/api/taxonomy` endpoint returns the JSON-serializable taxonomy; new `inject_taxonomy` Jinja context processor exposes it to templates; new `_macros.html` defines `tax_chip` and `sev_chip` macros; new `window.MS_TAXONOMY` + `msEntityChip()` + `msSevChip()` JS globals on every page. Every chip, badge, severity pill, and table column header on every page now derives from this single source — `iocs.html`, `asset_baseline.html`, `projects.html`, `dashboard.html`, `capture.html`, `scan_progress.html`, `assets.html`, `findings.html`, `audit.html` all refactored to consume taxonomy primitives instead of bespoke styling. **HP-HMI mode (ISA-101 / ASM Consortium / Hollifield 2008).** Toggle in the global nav and in the workbench lens-strip control bar (next to Risk Overlay / Assessment Report). Persisted in `localStorage`; applied early via `<html data-hmi="on">` so the SVG renders correctly on first paint. When on: equipment, normal-traffic edges, identity chrome, and Purdue-level color coding all desaturate to gray; CRITICAL and HIGH severity colors stay loud (alarm-actionable); MEDIUM / LOW / INFO desaturate (informational, not actionable); lens chips desaturate except the active one; SVG node fills branch on `nodeHasRisk(n)` so alarm-state assets retain their style color while normal-state assets get `var(--text-dim)` with reduced opacity and thinner stroke. Topology re-renders in-place via `ms:hmi-changed` custom event — no page reload. Designed for control-room wall-mount and dense-network triage; ICS engineering teams trained on ISA-101 expect this discipline. **Comprehensive doc refresh.** New `docs/taxonomy.md` (operator + developer reference). `docs/workbench-guide.md` rewritten with v3.5 layout, lens chip strip table, inspector + drawer sections, and a full HP-HMI Mode section with side-by-side default-vs-HP-HMI screenshots. README highlights extended. **i18n:** 24 new taxonomy keys + 50 workbench reshape keys + 2 HP-HMI keys, en/fr parity preserved (1158 keys total). **Tests:** 192 pass (172 from prior + 20 new for taxonomy module + run_store/recovery already in place). 23 new screenshots in `docs/screenshots/v3.5-after/` covering every lens (default + HP-HMI), inspector, drawer-Findings / drawer-Conversations / drawer-Assets, and the global pages refactored for taxonomy. **Why:** v3.4.0 closed the durability gap (mid-scan recovery); v3.5.0 closes the analyst-experience gap. Defenders coming from Dragos / Claroty / NetworkMiner / Wireshark expect map-first, expect a formal entity model, expect ISA-101 discipline. The platform now meets that expectation. **Out of scope, flagged for follow-up:** map legend redesign for HP-HMI mode (it currently still shows the full Purdue color key when HP-HMI is on, which is misleading); per-tenant brand customization for cloudmarlin SaaS launch; trends-over-snapshots (rolling line charts replacing instant-readout chips — the second ISA-101 fundamental MarlinSpike doesn't yet honor); time-series asset metrics for trend rendering; STIX/MISP IOC import. |
| v3.4.0 | 2026-05-09 | **Mid-scan recovery — scans no longer get stuck `running` after a Flask restart.** New module `marlinspike/run_store.py` persists the recovery essentials (engine PID, engine argv, hard deadline) to `scan_history` at scan launch. New module `marlinspike/recovery.py` runs once on every `create_app()` boot: walks `scan_history WHERE status='running'`, then for each row probes PID liveness with a PID-reuse defense (`/proc/<pid>/cmdline` on Linux, `ps -p <pid> -o command=` on macOS, with a distinctive-token match against the saved argv). **Four reconciliation outcomes:** *reattached* (PID alive + argv matches → spawn watcher thread that polls until engine exits and ingests report), *reaped_completed* (PID dead but report file is well-formed → ingest node/edge counts, mark completed), *reaped_failed* (PID dead, report missing/partial → mark failed with diagnostic `error_tail`), *reaped_abandoned* (past `timeout_at` → mark failed). The blanket "mark all running scans as interrupted on boot" sweep is replaced by this per-row reconciliation. Five new `scan_history` columns (all nullable, materialised by the existing per-column ALTER TABLE migration loop): `pcap_path`, `engine_pid`, `engine_argv` (JSON-encoded), `timeout_at`, `recovery_state`. Scan-start path now goes through `run_store.record_start()` which captures `proc.pid` + the full argv at launch; chunked scans call `run_store.update_pid()` per child so recovery sees the currently-running stage. **Cross-worker concurrency-check option** for cloudmarlin's horizontal-scale launch: setting `MARLINSPIKE_RUN_STORE=db` routes `_get_active_runs(user_id=...)` through `scan_history` instead of the per-process `_run_registry` dict — required when running `gunicorn -w >1` so per-tier limits are correct across workers. Default remains `memory` (legacy in-process, single-worker safe). **Status endpoint fallback:** `/api/runs/<id>/status` now falls back to `scan_history` when the in-memory registry doesn't have the run (post-restart polling); response includes `recovered: true` and `recovery_state` so the UI can show "this scan was reconciled from durable state". Two new env vars: `MARLINSPIKE_RUN_STORE` (`memory`/`db`, default `memory`), `MARLINSPIKE_SCAN_TIMEOUT_S` (default 3600, set 0 to disable abandonment reaping). New operator doc `docs/run-store-and-recovery.md`. **Tests:** 27 new (9 run_store + 18 recovery), 172 total pass. **Why:** "Flask restarts shouldn't lose your scan" is a baseline reliability expectation — the engine subprocess was already surviving (reparented to init/launchd), but marlinspike couldn't find it again. Closing this gap also unlocks cloudmarlin's horizontal scale story (the cross-worker concurrency-check fix is half the launch blocker; queue-based engine distribution is the other half, planned for v3.5.x). |
| v3.2.1 | 2026-05-09 | **CSP nonce support in all templates.** All `<style>` and `<script>` block elements across the 18 standard templates now carry `nonce="{{ csp_nonce }}"` so downstream wrappers that emit per-request nonce-based CSP headers (eliminating `'unsafe-inline'`) work without shadowing templates. The `{{ csp_nonce }}` variable is resolved via the host app's Jinja context processor — marlinspike itself does not generate or manage nonces. Template-only change; no schema migration, no API surface change. **Remaining limitation:** the 166 inline event handlers (`onclick=`, `oninput=`, `onchange=`) and 452 `style="..."` attributes in the templates still require `'unsafe-inline'` in the CSP when marlinspike routes are served; eliminating them is a separate refactor tracked as a follow-up. |
| v3.3.0 | 2026-05-07 | **Live capture (optional, Linux only).** New `marlinspike-capd` sidecar daemon (`marlinspike-capd/`, ~600 LOC of Python) holds `CAP_NET_RAW` and supervises `dumpcap` with ring-buffer rotation; the unprivileged web app talks to it over a unix-domain socket guarded by `SO_PEERCRED` (Linux) / `LOCAL_PEEREID` (macOS dev). Three-mode deployment: not installed (default, no caps anywhere); bundled compose service under `--profile capture` with `network_mode: host` and shared `capd-socket` / `capd-captures` volumes; native systemd unit (`marlinspike-capd/systemd/marlinspike-capd.service` — hardened: `AmbientCapabilities`, `ProtectSystem=strict`, `NoNewPrivileges`, `SystemCallFilter=@system-service`). New uds JSON-RPC: `list_interfaces`, `validate_bpf` (compiles via `pcap_compile_nopcap` against the active interface's DLT — `EN10MB` for real NICs, `LINUX_SLL2` for `any`), `start`, `stop`, `stats` (streams once-per-second frames). New `CaptureSession` model and `/api/capture/*` blueprint: health, interfaces, validate-bpf, sessions CRUD, SSE `/sessions/<id>/stream`. **Rotation consumer** wired to the existing `MARLINSPIKE_ENGINE_CMD` path — every closed PCAP gets a `--fast` chain run and lands as a regular project report. **Per-project saved-filter library** (`SavedFilter` model, `(project_id, name)` unique constraint, GET/POST/DELETE `/api/capture/filters`). **Concurrency:** `LIVE_CAPTURE_MAX_CONCURRENT` per host (default 2), per-interface lock with `any` blocking all real ifaces and vice versa. **Admin override:** admins can stop any user's session (audit event `capture.admin_stop` records original owner). New workbench page at `/capture` (`templates/capture.html`, ~580 LOC) with capd-status pill, project picker, virtual/physical interface filter toggle, debounced live BPF validation, ring config, saved-filter dropdown with save/delete affordances, active-sessions panel driven by `EventSource`, and history table. Existing `templates/live.html` (in-progress scan-progress viewer) renamed to `templates/scan_progress.html` to free the `live` namespace. Web app degrades gracefully when capd is unreachable: `/health` returns `reachable: false`, capture-write endpoints return 503 with a useful error, and a failed start still creates a `status=failed` row with `error_tail` so attempts are auditable. New env vars: `LIVE_CAPTURE_ENABLED` (default `false`), `LIVE_CAPTURE_SOCKET`, `LIVE_CAPTURE_TIMEOUT_S`, `LIVE_CAPTURE_MAX_CONCURRENT`. **i18n:** 38 + 7 = 45 new keys covering nav, page, status, filters, save/delete prompts; en/fr parity preserved. Two new tables: `capture_sessions`, `saved_filters` (auto-`db.create_all()` on startup). `INSTALL.md` extended with three-mode deployment + verification commands; `COMPATIBILITY.md` adds the live-capture platform matrix and declares the uds JSON-RPC the capd↔web compatibility boundary. New operator guide at `docs/live-capture.md` (BPF cookbook for OT, saved-filter workflow, troubleshooting). **Why:** the GrassMarlin parity gap was live capture; this closes it without compromising the web app's posture (capd holds caps, web app doesn't) and without committing to FATHOM's continuous multi-sensor model. Each rotated PCAP feeds the existing analysis chain, so live findings appear ~1 file rotation behind real time. **Out of scope, flagged for follow-up:** native libpcap inside `marlinspike-dpi` for sub-rotation-latency findings; tap aggregation across multiple hosts; UI surface for the saved-filter library on the upload-driven scan path (currently live-capture only); cleanup policy for old session capture directories (kept indefinitely by design — `keep` was the user-confirmed v1 choice). |
| v3.2.0 | 2026-05-05 | **Longitudinal asset baselines.** New per-asset full-screen page at `/projects/<pid>/assets/<asset_key>` that walks every report in the project and surfaces a single longitudinal profile for one asset — protocol-mix history per report, peer set with first/last-seen attribution, finding history with cadence ("seen in 14 of 28 reports"), L2/ARP anomaly cadence, vendor/role/device-type stability with drift detection, and the headline value: a **novelty-vs-baseline** card that lists what's new in the latest report (new protocols, new peers, new finding categories, lost peers/protocols) compared to everything before it. Pure compute over existing report JSON via the new `marlinspike/baselines.py` module — no schema change, no live capture. New endpoint `GET /api/projects/<pid>/assets/<asset_key>/baseline` (asset_key resolved MAC-first, IP-fallback) returns the structured profile; same endpoint accepts `?limit_reports=N` to cap the window. Click-through links from the workbench: new "Baseline" column on the Asset Ledger pane and a "Baseline" button next to Center/Clear in the Selected Asset sidebar. **i18n:** 40 new keys (37 `baseline.*` + 3 `viewer.baseline.*` / `viewer.sel.baseline`), en/fr parity preserved (1086 keys total). **Tests:** 145 pass (+26 from `tests/test_baselines.py`). **Why:** v3.1.0 closed the responder loop for "what just happened in this capture"; v3.2.0 closes it for "is this asset behaving differently than it has been." Defenders showing up to a long-running site engagement get a per-asset longitudinal profile without needing a live-capture engine — the cross-report aggregation pattern from v2.4.0's Project Overview, focused on a single asset. **Out of scope, flagged for follow-up:** notifications on novelty (scan-completion webhook firing when an asset's profile diverges — separate v3.3.0 batch), trend sparklines in the protocol-mix table (current rendering is per-report columns; long-term-trend visualisation deferred). |
| v3.1.0 | 2026-05-04 | **Major release — workbench redesign, English/French i18n, responder workflow loop, package restructure, downstream extension hooks.** *(Supersedes v3.0.0, which shipped 24 hours earlier and absorbed into this entry.)* **Two new workbench panes** driven entirely by existing report JSON: **Traffic Statistics** (capture-volume KPIs, top-25 conversations by bytes with src/dst pivots, protocol byte distribution, top source/destination endpoints, conversation-anomaly flags for beaconing / high-entropy DNS / unsecured OPC) and **Protocol Drilldown** (per-dissector evidence for Modbus, S7, DNP3, IEC 60870-5-104, CIP, MMS, GOOSE, BACnet, OMRON, PROFINET, OPC, DNS — with a Protocol Roster header chip set that lists silent families for completeness so the absence of a protocol is itself part of the report). Each active family renders a function-code/object-group/typeID breakdown chip row and a top-10 src→dst pair table with click-to-pivot endpoints. **Responder workflow loop — time-window scrubbing + sub-PCAP carve-out.** Adaptive packet-rate histogram below the workbench toolbar, computed from `report.conversations[].first_seen/last_seen` (~120 buckets, bucket size scales with capture span). Drag-to-select a time window; every conversation-driven pane filters live via a new `timeFilteredConversations()` helper. New "Extract" button column on the Top Conversations table and Protocol Drilldown pair tables — POSTs to `/api/reports/<filename>/extract`, which shells out to tshark + editcap inside the container, honours the active time window, caps output at 500 000 packets and 60 s wall-clock, and streams the result back as `application/vnd.tcpdump.pcap`. Companion endpoint `GET /api/reports/<filename>/timeline` returns the bucketed histogram with an LRU-32 cache keyed by report path + mtime. **Asset context layer.** Two new tables — `asset_tags` (project-scoped, MAC-first/IP-fallback key, columns: owner, criticality, zone, business function, free text) and `finding_notes` (project-scoped, keyed by stable finding signature = sha256-hex-32 of `(category, sorted nodes, sorted edges)`, columns: status ∈ {open, investigating, resolved, false_positive}, body, author). New CRUD API at `/api/projects/<pid>/asset-tags` and `/api/projects/<pid>/notes`. `_apply_contextual_severity()` overlays a `contextual_severity` field on every risk finding: bumps one tier (cap CRITICAL) when any affected asset is tagged `critical`, drops one tier (floor INFO) when *all* affected assets are tagged `low`. Findings render with the contextual severity and a small "→ CRITICAL (asset criticality)" badge. Selected Asset sidebar gains an editable Asset Context section. Findings tied to a saved note display the note status pill + body inline with an "Edit note…" affordance. **IOC threat-hunting.** Two new tables — `ioc_lists` and `ioc_entries` (typed: `ip` / `mac` / `oui` / `domain` / `sha256` / `md5`, optional label + severity). CRUD API at `/api/projects/<pid>/iocs`. Bulk-paste endpoint auto-detects type per line (IPv4/v6, MAC/OUI/sha256/md5 via regex, domain fallback), idempotent on the unique constraint. Cross-report scan walks every report in the project, matches across nodes/conversations/c2_indicators/risk_findings/dns_queries/malware_findings; returns hit table with severity/type summary, capped at 1000 hits with a `truncated` flag. New `/iocs` page with two-column layout (list manager + detail/import/scan), nav link next to Projects. **Bilingual UI (English / Français)** via a homegrown JSON-dictionary i18n layer with no new pip dependencies — `marlinspike/i18n.py` loads per-locale dictionaries from `marlinspike/translations/`, lookups fall back to English, missing keys return the key itself. `before_request` resolver (session → `Accept-Language` → default), `/i18n/set/<locale>` endpoint with same-host redirect guard, `t()` Jinja global, plus a JS bridge (`var I18N = {{ i18n_dict() | tojson }}` + `tjs()` shim) so JS-rendered surfaces in `viewer.html` flip language alongside the Jinja-rendered ones. 1046 keys covering all Jinja chrome and the entire JS-rendered viewer (header KPI strip, toolbar, left-rail, all 10 panes, Selected Asset / Operator Snapshot / Investigation Queues / Top Findings / Protocol Mix / L2 sidebar / Asset Context / IOC management panels, Assessment Report tab, MAC table, capture provenance chips, timeline, finding-note prompts). Server-side localization helper overlays translated category labels, remediations, and description templates on engine-emitted `risk_findings` and `c2_indicators` so finding content flips between EN/FR. 21 engine categories + 12 plugin categories labeled in FR; 8 highest-frequency engine categories get FR descriptions and remediations. French `README.fr.md` ships as a peer to `README.md`. **Provenance polish + L2/ARP anomaly surface:** fixed a long-standing header bug where `summary.packet_count` / `duration_seconds` read keys that never existed on engine-emitted reports — the header had been silently empty since v2.x. Both naming conventions now accepted. New header chips for capture source, link type, unique MAC count. New `L2 / ARP Anomalies` sidebar panel surfaces bilgepump `mac_local` / `arp_spoof` / `mac_flap` detections (already produced by engine v2.0.9 but dropped by the workbench), grouped by anomaly_type with per-bucket count, severity histogram, and representative sample (capped at 8 buckets, walks ≤5000 entries). **Package restructure — `marlinspike/` is now a proper Python package.** All application source moved into `marlinspike/`: `_aggregate.py` → `marlinspike/aggregate.py`, `_audit.py` → `marlinspike/audit.py`, `_auth.py` → `marlinspike/auth.py`, `_config.py` → `marlinspike/config.py`, `_i18n.py` → `marlinspike/i18n.py`, `_models.py` → `marlinspike/models.py`, `_ms_engine.py` → `marlinspike/engine.py`, `app.py` → `marlinspike/app.py`. `templates/`, `static/`, `translations/` move inside the package. Legacy top-level `marlinspike.py` replaced by `marlinspike/__main__.py`, invoked as `python -m marlinspike`. New `pyproject.toml` (hatchling) makes the project pip-installable; `pip install marlinspike` exposes the `marlinspike` package and the bundled `plugins.marlinspike_*` namespace. `__version__ = "3.1.0"`. `BASE_DIR` kept as alias for `PROJECT_ROOT` (parent of the package directory, env-overridable via `MARLINSPIKE_PROJECT_ROOT`); per-asset env vars `MARLINSPIKE_DATA_DIR` / `MARLINSPIKE_RULES_DIR` / `MARLINSPIKE_PRESETS_BAKED_DIR` for installed-wheel deployments. Engine subprocess invocation switches from `python -u marlinspike.py` to `python -u -m marlinspike` via `MARLINSPIKE_ENGINE_CMD`. Top-level compat shims deleted. **Two downstream-wrapper extension hooks:** `@csrf_exempt` decorator in `marlinspike.auth` marks a view as exempt from the global Origin/Referer CSRF check (Stripe / OAuth / SCIM webhooks); `marlinspike.app.set_concurrent_check_fn(fn)` lets wrappers replace the global single-scan limit with per-user / per-tier policy. Default behaviour is unchanged when no hook is installed. **Schema migration:** auto-`db.create_all()` on startup creates the four new tables (`asset_tags`, `finding_notes`, `ioc_lists`, `ioc_entries`); no column-level migration for existing tables. Dockerfile copies the `marlinspike/` package and runs `pip install --no-deps .`; `deploy.sh --ui-only` rsyncs the package directory in place. **Tests:** 119 pass — 14 timeline + 19 asset-context + 35 IOCs + 41 pre-existing baseline + 10 aggregate. **Migration:** see `UPGRADING.md`. **Why:** v3.0.0 (which shipped a day earlier under a separate tag) brought the workbench into a proper IR-triage flow that lines up with the NetworkMiner / Arkime / Wireshark / Corelight / Dragos consensus, made the project pip-installable so downstream wrappers (cloudmarlin) can extend without forking, and added bilingual coverage. Within 24 hours we identified the next responder gap — the workbench terminated at the finding, with no time-scrubbing, no flow extraction to Wireshark, no asset-criticality weighting, no IOC overlay — and v3.1.0 closes the loop. The v3.0.0 tag is preserved in git history; the prose has been consolidated here. **Out of scope, flagged for follow-up:** viewer-side IOC highlighting (paint matched IPs/MACs in the workbench when a viewer is opened with a list-id query param — storage and scan API are in place; only the highlighting layer remains), STIX 2.1 / MISP IOC import (paste-list MVP shipped instead), note-status filter on findings panes. |
| v2.4.0 | 2026-04-27 | **Project Overview tab — cross-report aggregation.** New `/api/projects/<id>/aggregate` endpoint walks every report in a project, dedupes assets (MAC-keyed, IP fallback) and findings (`(category, sorted(affected_nodes), sorted(affected_edges))`) across captures, and returns a single roll-up document. Asset and finding records carry `first_seen_report` / `last_seen_report` attribution; finding severity promotes to the highest seen across reports while the latest report wins for description. Aggregation is pure compute — no schema migration, no DB writes — and consumes plugin findings via the existing `_load_report_with_extensions` bridge so APT and ARP findings roll up alongside engine findings. New "Overview" tab (now the default landing tab on a project) renders: KPI strip (reports rolled up, unique assets, distinct findings, protocols observed) with a danger accent stripe when CRITICAL/HIGH > 0; severity distribution bar with inline counts (`8 CRITICAL`, `4 HIGH`, …) and a five-class legend; deduped findings table sorted by severity then occurrence count; asset inventory with sticky filter toolbar (MAC/IP/role/vendor substring search) and bounded scroll; protocol distribution and ATT&CK coverage chips. Workspace hero hidden on Overview to avoid duplicate framing. Aggregator unit-tested across 10 cases (per-report dedup, cross-report dedup, severity promotion, IP-fallback keys, ATT&CK rollup, scan-profile breakdown, loader failure isolation). Playwright smoke covers login → project select → Overview render → KPI assertions → severity bar segments → findings sort → asset filter → tab cycling, 16/16 assertions passing on the 4SICS benchmark project (2 reports, 42 assets, 12 distinct findings, 10 protocols). |
| v2.3.0 | 2026-04-25 | **APT lateral-movement & C2 detection plugin (`marlinspike-apt`) + plugin findings merged into engine `risk_findings`.** New sidecar plugin auto-runs after every scan and emits a JSON artifact with six finding categories: `APT_LATERAL_MOVEMENT_SMB`, `APT_LATERAL_MOVEMENT_RDP`, `APT_LATERAL_MOVEMENT_WINRM`, `APT_OT_RECONNAISSANCE`, `APT_NEW_HOST_PROTOCOL`, `APT_C2_BEACON` — mapped to ATT&CK Enterprise T1021.001/002/006, T1018, T1046, T1071, T1102, T1573, T1570 and ATT&CK ICS T0840, T0842, T0888. Tier 1 detectors operate on existing report fields with no engine widening; C2 beacon detector consumes engine-emitted `beacon_score`/`beacon_interval`/`beacon_jitter` per conversation. Site-tunable thresholds and per-rule enable/disable via YAML rule packs in `rules/apt/` (jump host allowlist, OT polling source allowlist, polling source role allowlist for HMI/historian/engineering-workstation/transit devices). **Plugin-to-engine bridge:** `_load_report_with_extensions` now translates plugin findings (category, severity, src_ip, distinct_target_ips, attack_techniques) into the engine's `risk_findings` schema and merges them into the report — so APT findings (and ARP findings from v2.2.0) drive dashboard severity counts, top-findings lists, priority-node attribution, and per-node `_riskFindings` views, not just the workbench panel. Workbench view registered under Risk tab with metric strip, ATT&CK chip list, and per-detector tables (lateral movement, OT reconnaissance, NHP, C2 beacon candidates). Auto-run wiring in `app.py` (capabilities catalog updated with the six new categories) and configuration toggle in `_config.py` (`MARLINSPIKE_APT_ENABLED`, `MARLINSPIKE_APT_RULES`). FP reduction: industrial transit devices (network switch/router/industrial gateway) excluded from `new_host_protocol` role expectations and added to OT-reconnaissance polling-source role allowlist. Engine field interop fix: plugin reads conversation `port` (engine canonical) with `dst_port` fallback. 24 unit tests covering all six detectors, rule-pack overrides, allowlists, role suppressions, and the port-field alias regression. End-to-end validated on synthetic scapy PCAP (6 findings across 5 detectors, 0 false categories) and 4SICS real OT capture (0 findings, 0 false positives after FP fixes). |
| v2.2.0 | 2026-04-25 | **ARP poisoning detection plugin (`marlinspike-arp`).** New sidecar plugin auto-runs after every scan and emits a JSON artifact with six finding categories: `ARP_DUPLICATE_IP_CLAIM`, `ARP_GATEWAY_MAC_CHANGE`, `ARP_MAC_CLAIMS_MANY_IPS`, `ARP_SCAN_BEHAVIOR`, `ARP_BROADCAST_STORM`, `ARP_GRATUITOUS_REPLY` — mapped to ATT&CK ICS T0830, T0814, T0842, T1557.002. Detectors fuse three signal sources and dedupe across them: ARP conversations from the existing chain, per-packet `arp_observations` (tshark path, opcode-aware scan ratio + gratuitous reply detection), and engine-emitted `l2_anomalies` from the bilgepump consumer (Rust DPI path, where ARP doesn't surface as `protocol_transaction` events). Site-tunable thresholds and per-rule enable/disable via YAML rule packs in `rules/arp/`; `gateway_ip` configurable per site for gateway-MAC-change watch. Auto-run wiring in `app.py` (capabilities catalog updated with the six new categories) and configuration toggle in `_config.py` (`MARLINSPIKE_ARP_ENABLED`, `MARLINSPIKE_ARP_RULES`). 17 unit tests covering all detection paths and dedupe behavior. End-to-end validated on a 7-scenario PCAP against both Python tshark and Rust DPI substrates with equivalent finding output. |
| v2.1.0 | 2026-04-16 | **UI/UX polish sprint.** Animated loading spinners replace all "Loading..." text placeholders. Toast notification system upgraded with severity levels (info/success/warning/error), icons, and color coding — backward compatible with existing `msToast(msg, true)` calls. Centralized empty state component (`msEmptyHTML`) with SVG illustrations and call-to-action links across dashboard, reports, scans, and projects. Modal dialog system (`msModal.open/close`) with backdrop, focus trapping, escape-to-close, and slide animation — Users page admin panels (add user, upload limits, change password) converted from `display:none` panels to proper modals. Form styling consistency: `.ms-select` fixed (was broken CSS Modules `composes`), focus transitions added to all input types. Info-hint tooltip system moved to `base.html` for global availability; new tooltips on Capabilities page (Source, Type, Family filters) and Reports page (baseline drift). Responsive breakpoints in `base.html`: nav bar, content wrapper, tables, and modals adapt at 768px and 480px; reports/scans tables scroll horizontally on mobile. **Admin audit log viewer** (`/audit`): paginated, filterable table of all auth events (logins, failures, token usage) with debounced search by username/event type and status filter. API endpoint `GET /api/audit` with offset pagination, event type/username/status filters. ICS ATT&CK domain chip gets distinct red styling in the report viewer's MITRE section. **CSV export** added to report viewer: "CSV Assets" exports all nodes, "CSV Findings" exports all risk findings. |
| v2.0.8 | 2026-04-15 | **ICS ATT&CK support + multi-pack MITRE loading.** Findings catalog and MITRE plugin dispatch load all `*.yaml` packs from `rules/mitre/`. ICS domain catalog lookups, per-rule domain labeling, tactic shortname fallback. Version label shows both Enterprise and ICS ATT&CK versions. |
| v2.0.7 | 2026-04-13 | **Rust DPI default + benchmark update.** Scan handler now passes `--dpi-engine auto` for both direct and chunked paths. DPI engine and binary path configurable via env vars. README benchmark updated to 1.7 GB / 58 seconds. |
| v2.0.6 | 2026-04-12 | **Auth hardening (companion to engine release).** Password reset API endpoints, session invalidation, audit logging UI infrastructure. |
| v2.0.4 | 2026-04-11 | **Remove silent PCAP truncation from upload handler.** Full PCAP saved to disk; chunked pipeline handles large files. Upload limit label updated. |
| v2.0.3 | 2026-04-11 | **Docs cleanup.** Stale "Current release target: 2.0.0" banner fixed to reflect actual release. `INSTALL.md` "Live Capture" section replaced with a "Scope" section that clarifies MarlinSpike is a PCAP analysis tool and points live-capture interest at FATHOM. Migration spec updated to drop the removed `list-interfaces` subcommand. `releases-live.md` removed from the repo (historical deployment log that no longer belongs in the public tree). |
| v2.0.2 | 2026-04-11 | **Live capture removed from web UI.** Removed the Live Capture scan tab, the `/api/interfaces` endpoint, the `loadInterfaces()` JS, and the `ENABLE_LIVE_CAPTURE` config flag. Removed `iproute2` from the Dockerfile (no longer needed without interface enumeration). Scan handler now requires a PCAP file. README and configuration docs updated to reflect MarlinSpike's positioning as a PCAP analysis tool — live capture, multi-sensor monitoring, and centralized OT collection are out of scope and pointed at FATHOM. |
| v2.0.1 | 2026-04-10 | **Admin UX + topology map legibility.** Upload limit (MB) field added to admin user creation form — previously only editable after creation. New role legend with live counts on the live topology view sidebar and report viewer sidebar, plus a map key explaining circle size (connection count), solid/dashed edges (read/write traffic), and Purdue band layout. Info tooltips (`ⓘ`) added to scan configuration options: Command, Profile, Capture filter (BPF), and Skip ephemeral ports — hover for analyst-oriented guidance. New role styles for Data Server, Infrastructure Server, Enterprise Server/Workstation/Host (with service-specific device types). |
| v2.0.0 | 2026-03-27 | **Release 2.0.0 — analyst workspace + live-ready large-PCAP flow.** Dashboard, projects, and scans now expose canonical chain commands with explicit `fast` / `full` profiles, retries preserve the chosen profile, and scan history records it. The backend normalizes legacy command labels, adds chunked file-based chain execution for large captures (`split -> dissect chunks -> merge -> chain-from-conversations`), improves stop handling for both single-process and chunked runs, and keeps MITRE sidecar generation attached to successful full-chain reports. Includes a live Playwright end-to-end smoke path for login -> scan -> viewer verification. |
| v1.9.0 | 2026-02-25 | **User profiles + per-user upload limits.** New `/profile` page: editable full name, company, email, phone, birthday, address. Read-only stats: upload limit, scan count, project count, join date, role. Password change moved to profile page. New `GET/POST /api/profile` endpoints. Per-user `upload_limit_mb` (default 200) replaces hardcoded global limit in upload handler — server enforces each user's limit, `projects.html` label and client-side check fetch from `/api/profile` dynamically. New `POST /api/users/<username>/limits` admin endpoint to set `upload_limit_mb`. Users table gains an Upload Limit column with inline editing. Nav username is now a link to profile. Postgres schema migration runs on startup (`ALTER TABLE users ADD COLUMN IF NOT EXISTS` for the new profile and limit fields). Requires Cython rebuild (`_models.py` updated) |
| v1.8.2 | 2026-02-25 | **Bugfix:** "Show log" button on scan panels now only rendered for admin users. Non-admin users no longer see the log toggle during active scans. `IS_ADMIN` injected via Jinja into dashboard JS |
| v1.8.1 | 2026-02-25 | Initial separate web UI versioning (dashboard truncation, dedicated Reports + Scans pages) |

## v1.7.0 Performance — Before & After

200 MB PCAP — 4SICS-GeekLounge-151022.pcap (2,274,747 packets)

| Metric | v1.4.0 (before) | v1.7.0 (after) | Improvement |
|--------|-----------------|----------------|-------------|
| **Total time** | 35 min | **2 min 56 sec** | **12x faster** |
| **Peak memory** | 3 GB (OOM kill) | **552 MiB** | **5.6x less, no crash** |
| **Idle memory** | 2 GB+ stuck | **80 MiB** | **25x less** |
| **Conversations** | 473,000 raw | **2,205 unique** | **215x reduction** |
| **Concurrent users** | 0 (crashes) | **4-5 feasible** | sequential queue, ~500 MiB/scan |

Confirmed 2026-02-25 on a 3 GB RAM Debian 13 deployment with Docker and tshark 4.4.13.

## Backup

`backups/marlinspike-v1.7.0.py` — engine snapshot before Stage 1/2 optimization (inline collapse + deferred metadata).
`backups/marlinspike-v1.6.0.py` — engine snapshot before security hardening + scalability overhaul.
`backups/marlinspike-v1.4.0.py` — engine snapshot before Stage 3/4 performance fix.
`backups/marlinspike-v1.0.0.py` — engine snapshot before L2 additions.
